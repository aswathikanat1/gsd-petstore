---
phase: 4
plan: 04-02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - package.json
  - test/petLifecycle.test.ts
  - .github/workflows/validate.yml
  - .github/workflows/docs.yml
  - docs/getting-started.md
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - Contract tests run through a single standard script locally and in CI.
    - Release gating fails closed when build/lint/docs/contract tests fail.
    - v1 endpoint coverage and critical edge cases are automated and repeatable.
  artifacts:
    - package.json includes explicit `test` (and if needed `test:contract`) scripts.
    - test/petLifecycle.test.ts covers all v1 endpoint families and critical negative paths.
    - CI workflow(s) execute build, openapi lint, docs generation, and test scripts.
    - docs/getting-started.md documents final verification commands for release readiness.
  key_links:
    - Local and CI verification commands are aligned and reproducible.
    - OPS-02 is satisfied through executable, automated contract tests.
    - Release verification behavior is deterministic and merge-blocking.
---

# Plan 04-02: Contract Test Suite and Release Verification Workflow

<objective>
Finalize release readiness by enforcing automated contract-test coverage and CI release gates that verify the full v1 API surface before ship.
</objective>

<tasks>

## Task 1: Standardize test execution scripts and endpoint contract coverage

<read_first>
- package.json - existing script definitions and missing `test` script gap
- test/petLifecycle.test.ts - current endpoint test coverage baseline
- petstore.yaml - full v1 operation list and critical edge-case responses
- .planning/phases/04-hardening-and-release-readiness/04-CONTEXT.md - D-04, D-05, D-06
</read_first>

<action>
Add explicit local test script(s) and expand endpoint tests to cover all v1 operation families and critical negative paths. Ensure assertions validate both status codes and response payload contract shapes.
</action>

<acceptance_criteria>
- [ ] package.json exposes a working `test` script for local/CI execution.
- [ ] Contract tests cover all implemented v1 operation families.
- [ ] Critical edge cases (invalid input, missing resources, unauthorized paths, default failures) are asserted.
- [ ] Test outputs are deterministic and suitable for merge-gate enforcement.
</acceptance_criteria>

## Task 2: Enforce final release gates in CI and document verification flow

<read_first>
- .github/workflows/validate.yml - current OpenAPI lint gate
- .github/workflows/docs.yml - current docs generation gate
- docs/getting-started.md - developer command reference for local validation
</read_first>

<action>
Update CI workflows so release gating enforces build, OpenAPI lint, docs generation, and test execution in a fail-closed manner. Update existing developer docs with the final verification command sequence used before release.
</action>

<acceptance_criteria>
- [ ] CI executes build + openapi lint + docs generation + tests on pull requests.
- [ ] Failing any required check blocks merge/release.
- [ ] Local verification steps match CI command sequence.
- [ ] OPS-02 is satisfied by executable automation rather than manual-only checks.
</acceptance_criteria>

</tasks>

<verification>
1. Run local release verification sequence (`build`, `openapi:lint`, `docs:generate`, `test`) and confirm success.
2. Intentionally break one contract assertion and confirm CI/test gate fails.
3. Restore contract behavior and confirm all gates pass end-to-end.
</verification>

<success_criteria>
- [ ] Team can run automated contract tests that verify request/response compliance for all v1 operations.
- [ ] Release workflow enforces deterministic quality gates prior to ship.
- [ ] Local and CI verification paths are aligned and documented.
</success_criteria>

<must_haves>
- Explicit test script available and used by CI.
- Contract tests include critical edge-case assertions.
- Release gate is fail-closed for required checks.
- Verification commands are reproducible and documented.
</must_haves>

<threat_model>
**Threats identified:**
- False confidence from partial or non-executable tests: mitigate with explicit scripts and full v1 coverage.
- CI drift from local developer verification: mitigate with shared command sequence.
- Merge of breaking contract changes due to missing gates: mitigate with fail-closed required checks.
- Undocumented verification steps causing inconsistent releases: mitigate by updating existing developer guide with final release sequence.
**ASVS Level:** L1
</threat_model>
